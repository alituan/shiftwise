node_modules/
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
.env*.local

# macOS
.DS_Store

# Uniwind auto-generates this from metro.config.js's dtsFile on first
# build; committed here already so tsc works without a live Metro process
# (see uniwind-types.d.ts) -- do not re-ignore it.
!uniwind-types.d.ts
